{{- if and .Values.namespace.create .Values.namespace.name }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "starter-app.namespace" . }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
{{- end }}
