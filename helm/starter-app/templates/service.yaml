apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.app.name }}-service

spec:
  type: NodePort

  selector:
    app: {{ .Values.app.name }}

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
